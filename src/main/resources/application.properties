# application.properties

# ????? - ?????????????????
spring.datasource.url=${DATABASE_URL:jdbc:mysql://mysql:3306/todo_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:todo_user}
spring.datasource.password=${DATABASE_PASSWORD:todo_pass}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ???????????
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# ?????
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# ========================================
# JPA?? - ??MySQL??
# ========================================
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ========================================
# H2??? - ??????????
# ========================================
spring.h2.console.enabled=false

# ========================================
# ?????
# ========================================
server.port=8080
server.tomcat.connection-timeout=5s

# ========================================
# ??????
# ========================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB